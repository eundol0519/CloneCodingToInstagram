// *** Header.js ***

import React from 'react';
import { Grid, Image, Button } from '../elements/Index';

const Header = props => {
  return(
    <React.Fragment>
    <Grid></Grid>
  </React.Fragment>
  );
};

export default Header;
